apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-green
  namespace: skillify
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
      version: green
  template:
    metadata:
      labels:
        app: backend
        version: green
    spec:
      containers:
        - name: backend
          image: nigachu42/backend:latest
          ports:
            - containerPort: 5000
